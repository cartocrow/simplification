#include "simplification_algorithm.h"

template<class Graph>
InputGraph* smoothGraph(Graph* graph, Number<MyKernel> radius) {
	InputGraph* result = copyGraph<Graph, InputGraph>(graph);

	return result;
}